name: ephemeral_bloc_example
description: "Runnable example for the ephemeral_bloc package."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  cupertino_icons: ^1.0.8
  ephemeral_bloc:
    path: ..
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
